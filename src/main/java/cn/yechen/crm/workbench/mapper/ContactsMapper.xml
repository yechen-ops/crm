<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yechen.crm.workbench.mapper.ContactsMapper" >
  
  <insert id="save" >
    insert into tbl_contacts (
      id,
      owner,
      source,
      customerId,
      fullName,
      appellation,
      email,
      mphone,
      job,
      birth,
      createBy,
      createTime,
      description,
      contactSummary,
      nextContactTime,
      address
    )
    values (
      #{id},
      #{owner},
      #{source},
      #{customerId},
      #{fullName},
      #{appellation},
      #{email},
      #{mphone},
      #{job},
      #{birth},
      #{createBy},
      #{createTime},
      #{description},
      #{contactSummary},
      #{nextContactTime},
      #{address}
    );
  </insert>
  
  <select id="getContactsListByFullName" resultType="cn.yechen.crm.workbench.domain.Contacts">
    select *
    from tbl_contacts
    <where>
      <if test="fullName!=null and fullName!=''">
        fullName=#{fullName}
      </if>
    </where>;
  </select>
</mapper>