<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yechen.crm.workbench.mapper.CustomerMapper" >
  
  <select id="getCustomerByName" resultType="cn.yechen.crm.workbench.domain.Customer">
    select *
    from tbl_customer
    where name=#{customerName}
  </select>

  <insert id="save" >
    insert into tbl_customer (
      id,
      owner,
      name,
      website,
      phone,
      createBy,
      createTime,
      contactSummary,
      nextContactTime,
      description,
      address
    )
    values (
      #{id},
      #{owner},
      #{name},
      #{website},
      #{phone},
      #{createBy},
      #{createTime},
      #{contactSummary},
      #{nextContactTime},
      #{description},
      #{address}
    );
  </insert>

  <select id="getCustomerNameList" resultType="String">
    select name
    from tbl_customer
    where name like '%' #{name} '%';
  </select>

  <select id="getIdByName" resultType="String">
    select id
    from tbl_customer
    where name=#{name};
  </select>
</mapper>