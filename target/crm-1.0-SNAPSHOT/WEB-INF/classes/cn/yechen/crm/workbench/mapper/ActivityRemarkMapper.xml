<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yechen.crm.workbench.mapper.ActivityRemarkMapper" >

  <sql id="Base_Column_List" >
    id, noteContent, createTime, createBy, editTime, editBy, editFlag, activityId
  </sql>

  <select id="getCountByIds" resultType="int">
    select count(*)
    from tbl_activity_remark
    where activityId in
    <foreach collection="array" item="id" open="(" close=")" separator="," >
        #{id}
    </foreach>
  </select>

  <delete id="deleteByActivityIds">
    delete
    from tbl_activity_remark
    where activityId in
    <foreach collection="array" item="id" open="(" close=")" separator="," >
      #{id}
    </foreach>
  </delete>

  <select id="getRemarkListByAId" resultType="cn.yechen.crm.workbench.domain.ActivityRemark">
    select <include refid="Base_Column_List"/>
    from tbl_activity_remark
    where activityId = #{activityId};
  </select>

  <delete id="deleteRemarkById">
    delete from tbl_activity_remark where id = #{id}
  </delete>

  <update id="updateRemark" >
    update tbl_activity_remark
    set
        noteContent = #{noteContent},
        editFlag = #{editFlag},
        editTime = #{editTime},
        editBy = #{editBy}
    where
        id = #{id}
  </update>

  <insert id="saveRemark">
    insert  into tbl_activity_remark (id,noteContent,createTime,createBy,editFlag,activityId)
    values (#{id},#{noteContent},#{createTime},#{createBy},#{editFlag},#{activityId})
  </insert>
</mapper>